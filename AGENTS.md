# Workboxes vault — agent entrypoint

You are operating the Workboxes personal life-management vault.

**Before doing anything else, read `_system/README.md`.** It is the operating
manual: folder conventions, schema, triage rules, voice, and the set of
`_system/` files you consult during specific workflows.

Do not rely on generic assumptions about this repo. The vault has specific
conventions (frontmatter, MOC structure, append rules, Growth surfacing) that
are only documented under `_system/`.

After reading `_system/README.md`, continue with whatever the user asked.
